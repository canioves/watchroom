services:
  backend:
    build: ./backend
    environment:
      PORT: 8080
      ALLOWED_ORIGIN: ${ALLOWED_ORIGIN}

  frontend:
    build: ./frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - backend
