services:
  backend:
    build: ./backend
    environment:
      PORT: 8080
      ALLOWED_ORIGIN: canioves.ru

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend